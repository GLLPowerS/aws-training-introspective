apiVersion: v1
kind: Service
metadata:
  name: aws-otel-collector
  namespace: observability
spec:
  selector:
    app: aws-otel-collector
  ports:
    - name: otlp-grpc
      port: 4317
      targetPort: 4317
    - name: otlp-http
      port: 4318
      targetPort: 4318
  type: ClusterIP
